#!/bin/bash

func () {
	echo "$1 is the first argument to func() "
        echo "$2 is the first argument to func() "
}

func cool code

echo "$1 is the first argument to the script"
echo "$2 is the first argument to the script"
